language: scala
dist: trusty
sudo: false
jdk:
- openjdk8
scala:
- 2.11.11
- 2.12.2
sudo: false
script: sbt ++$TRAVIS_SCALA_VERSION scalastyle coverage test coverageReport
after_success: bash <(curl -s https://codecov.io/bash)
cache:
  directories:
  - "$HOME/.ivy2/cache"
  - "$HOME/.coursier/cache"
  - "$HOME/.sbt/boot/"
before_cache:
- find $HOME/.ivy2 -name "ivydata-*.properties" -delete
- find $HOME/.sbt -name "*.lock" -delete
